language: php

php:
  # - '5.6'
  - '7.0'
  # - '7.1'

sudo: false

before_script:
  - composer install --no-interaction --prefer-dist
  - mv .env.example .env
  - php artisan key:generate

script:
  - vendor/bin/phpunit