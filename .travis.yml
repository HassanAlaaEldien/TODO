language: php

php:
  # - '5.6'
  - '7.0'
  # - '7.1'

sudo: false

before_install:
  - mysql -e 'CREATE DATABASE todo;'

services:
  - mysql

before_script:
  - composer install --no-interaction --prefer-dist
  - mv .env.example .env
  - php artisan key:generate
  - php artisan migrate

script:
  - vendor/bin/phpunit